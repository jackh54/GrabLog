{
  "name": "grablog",
  "version": "1.0.0",
  "private": true,
  "description": "Find and share Minecraft logs from any launcher in one curl",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.71",
    "@cloudflare/workers-types": "4.20250906.0",
    "typescript": "^5.8.3",
    "vitest": "~3.0.9",
    "wrangler": "4.35.0"
  },
  "engines": {
    "node": ">=20"
  }
}
